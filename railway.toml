# Railway does not support [services.*] multi-service format.
# Each service has its own railway.toml in its subdirectory:
#   keycloak/railway.toml
#   sync-backend/railway.toml
#
# In the Railway dashboard, set the Root Directory for each service
# to the matching subdirectory (keycloak or sync-backend).
